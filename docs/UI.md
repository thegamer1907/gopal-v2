# UI / UX

Screens, flows, and visual decisions, recorded as they firm up.

> **Status:** Not yet designed. The current screen is the minimal items skeleton (a list
> + an add form) used only to prove the stack. Real UI to be designed iteratively.

---

## Open decisions
- **UI approach (decide early):** stick with plain React + CSS (current template) vs.
  adopt a component library suited to data tables/forms (e.g. Mantine or shadcn/ui).
  Inventory UIs lean heavily on tables, forms, and dialogs.

## Screens
### Items (skeleton)
- A list of items (name + quantity) and a small form to add one. Temporary; validates
  the Go↔frontend round-trip. Will be replaced by the real inventory UI.

## Conventions
_To be established (layout, navigation, colors, component patterns)._
